# Server Configuration
server.port=${PORT:8080}

# Database Configuration (MySQL)
# Locally uses hardcoded values; on Railway, set env vars and they override these
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/wellnest?createDatabaseIfNotExist=true}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:root}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:6208gct}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# JWT Configuration
jwt.secret=${JWT_SECRET:5367566B59703373367639792F423F4528482B4D6251655468576D5A71347437}
jwt.expiration=86400000

# Mail Configuration (Gmail)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:ganpatstudy@gmail.com}
spring.mail.password=${MAIL_PASSWORD:cjgy vjuf uiqr eqbx}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
